# swarmagent in push-only mode: one pod per node, no ports exposed.
# It auto-detects Kubernetes and POSTs stats/events to the swarmbot /events
# endpoint with container ids in the "{namespace}/{pod}/{container}" format.
apiVersion: v1
kind: ServiceAccount
metadata:
    name: swarmagent
    namespace: swarmbot
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    name: swarmagent
rules:
    - apiGroups: [""]
      resources: ["pods", "nodes"]
      verbs: ["get", "list", "watch"]
    - apiGroups: ["apps"]
      resources: ["deployments", "statefulsets", "daemonsets", "replicasets"]
      verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
    name: swarmagent
roleRef:
    apiGroup: rbac.authorization.k8s.io
    kind: ClusterRole
    name: swarmagent
subjects:
    - kind: ServiceAccount
      name: swarmagent
      namespace: swarmbot
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
    name: swarmagent
    namespace: swarmbot
    labels:
        app: swarmagent
spec:
    selector:
        matchLabels:
            app: swarmagent
    template:
        metadata:
            labels:
                app: swarmagent
        spec:
            serviceAccountName: swarmagent
            tolerations:
                # Also run on control-plane nodes so their metrics show up.
                - key: node-role.kubernetes.io/control-plane
                  operator: Exists
                  effect: NoSchedule
                - key: node-role.kubernetes.io/master
                  operator: Exists
                  effect: NoSchedule
            containers:
                - name: swarmagent
                  image: swarmbot/swarmagent:latest
                  imagePullPolicy: IfNotPresent
                  env:
                      # Push-only contract: where to POST {"type":"stats"|"event"} envelopes.
                      - name: SWARMBOT_PUSH_URL
                        value: http://swarmbot.swarmbot.svc:8080/events
                      # Reported as message.id (the k8s node name).
                      - name: NODE_NAME
                        valueFrom:
                            fieldRef:
                                fieldPath: spec.nodeName
                  volumeMounts:
                      - name: rootfs
                        mountPath: /host
                        readOnly: true
            volumes:
                # Host filesystem for CPU/memory/disk stats.
                - name: rootfs
                  hostPath:
                      path: /
