node_modules
package-lock.json
**/dist
**/coverage
test-results
playwright-report
blob-report
**/.angular
apps/web/dist

# Verbatim copy of the claude.ai/design mockup — must stay byte-identical to the
# design source, so never reformat it.
design-qa/mockup
design-qa/tests/__screenshots__
design-qa/.netcache

# Git submodule with its own repo, toolchain and formatting rules. Reformatting
# it here would dirty the submodule without being committable from this repo —
# such changes belong in a PR against swarmbot-it/swarmagent instead.
swarmagent
